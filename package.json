{
  "name": "projectdashboard-deploy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix dashboard ci && npm --prefix dashboard run build",
    "start": "npm --prefix dashboard run preview -- --host 0.0.0.0 --port ${PORT:-3000}"
  }
}
